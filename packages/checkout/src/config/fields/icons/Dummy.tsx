import MonetizationOnIcon from '@mui/icons-material/MonetizationOn';
import type { SvgIconProps } from '@mui/material/SvgIcon';

const DummyIcon = (p: SvgIconProps) => {
  return <MonetizationOnIcon {...p} />;
};

export default DummyIcon;
