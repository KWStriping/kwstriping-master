mutation requestAccountDeletion(
  $channel: String!,
  # $redirectUrl: String!
) {
    requestAccountDeletion(
      channel: $channel,
      # redirectUrl: $redirectUrl
    ) {
      __typename
      # errors {
      #   ...Error
      # }
    }
  }
