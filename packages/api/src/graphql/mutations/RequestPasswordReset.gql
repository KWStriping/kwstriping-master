mutation requestPasswordReset(
  $email: String!
  # $redirectUrl: String!
  $channel: String!
) {
  requestPasswordReset(
    email: $email
    # redirectUrl: $redirectUrl
    channel: $channel
  )
  # {
  #   # __typename
  #   # errors {
  #   #   ...Error
  #   # }
  # }
}
